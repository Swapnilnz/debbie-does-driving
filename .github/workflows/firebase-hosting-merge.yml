# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    env:
      REACT_APP_PHONE_NUMBER: 0223544184
      REACT_APP_EMAIL: debbiedoesdriving@gmail.com
      REACT_APP_GTM: GTM-TLCBB6R
      DISABLE_BOOKING: true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEBBIE_DOES_DRIVING }}'
          channelId: live
          projectId: debbie-does-driving
